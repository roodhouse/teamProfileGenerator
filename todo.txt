            create a string of questions that never breaks out of the block 
            1. determine role
            2. goto that section of questions
            3. at end ask if they want to add another
                if yes go to the top
                if no then write the files
            4. never leave the block when answering yes or no to the above
            5. at the end of each block have the inquier/answers block all based on the same const for the questions