const Engineer = require('../lib/Engineer');

describe('Engineer', () => {
    it("should extend the employee class with github user name and methods for getGithub() and getRole()(override the parent class)", () => { 
      

      expect().toEqual();
    })
})