const Employee = require('../lib/Employee');

describe('Employee', () => {
    it("should return an object containing a name, id, email and also methods getName(), getId(), getEmail(), getRole()", () => { 
      

      expect().toEqual();
    })
})