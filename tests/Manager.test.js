const Manager = require('../lib/Manager');

describe('Manager', () => {
    it("should extend the employee class with officenumber and methods for getRole()(override the parent class)", () => { 
      

      expect().toEqual();
    })
})