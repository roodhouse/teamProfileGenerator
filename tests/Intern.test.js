const Intern = require('../lib/Intern');

describe('Intern', () => {
    it("should extend the employee class with school and methods for getSchool() and getRole()(override the parent class)", () => { 
      

      expect().toEqual();
    })
})